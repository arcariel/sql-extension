package com.company.sql.db.store.it;

import com.company.sql.db.store.runtime.ReactiveStoreProcedureService;
import io.quarkus.test.junit.QuarkusTest;
import jakarta.enterprise.inject.Instance;
import jakarta.inject.Inject;
import org.junit.jupiter.api.Test;

//@QuarkusTest
public class ServiceListingTest {

    /*@Inject
    Instance<ReactiveStoreProcedureService> allServices;*/

    /*@Test
    void testListServices() {
        allServices.stream()
                .peek(service -> System.out.println("Service: " + service))
                .count();
    }*/
}
